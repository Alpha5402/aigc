【第 2 轮 Prompt：基于样板页风格批量重构核心页面 + 全局收尾】

请继续优化“云上农管家 / AgriCloudManager”的前端。第 0 轮已经确定视觉方向，第 1 轮已经完成设计系统、通用组件和“我的地”样板页。现在请基于已经确认的样板页风格，继续优化其余所有页面，并完成整体收尾。

本轮目标：
在不破坏功能的前提下，把项目整体提升为一个视觉统一、交互顺滑、移动端优先、适合比赛展示、让人眼前一亮的智慧农业 App。

本轮页面：

* 添加作物页。
* AI 智能问诊页。
* 行情页。
* 销路匹配页。
* 感兴趣商户页。
* 营销助手页。
* 通知中心。
* 登录 / 注册页。
* 运营轻后台 H5 页面。
* 语音助手入口，如果存在。
* 全局样式一致性检查。
* 素材与版权整理。
* App / H5 兼容检查。

一、总体要求

请严格继承：

1. 第 0 轮最终视觉方向。
2. 第 0 轮 Design Tokens。
3. 第 0 轮页面结构重设计策略。
4. 第 0 轮素材策略。
5. 第 0 轮 canvas 使用策略。
6. 第 1 轮通用组件。
7. 第 1 轮“我的地”样板页风格。

本轮不要重新设计一套风格，不要推翻样板页，不要把不同页面做成不同 App。

整体效果要求：

* 美观。
* 自然。
* 专业。
* 有农业主题。
* 有真实 App 产品感。
* 有适合比赛展示的视觉亮点。
* 可以在农业主题范围内更炫酷一些。
* 交互顺滑。
* 信息层级清晰。
* App 端优先。
* H5 端兼容。

严禁：

1. AI 模板风。
2. 蓝紫霓虹科技风。
3. 通用 SaaS 工具风。
4. 传统后台风。
5. 土味农业网站风。
6. 廉价促销风。
7. 偏离智慧农业主题。
8. 素材风格混乱。
9. 伪造业务数据。
10. 为了好看破坏功能。

二、允许重构页面结构，但必须保留功能

本轮允许对各页面整体结构进行重新设计。不要被原来的表单、列表、表格样式限制。可以把页面改成更有 App 质感的看板、工具流、推荐页、结果页、消息中心或轻后台页面。

允许：

1. 调整模块顺序。
2. 重组信息层级。
3. 列表改卡片。
4. 表单改分组表单。
5. 弹窗改底部抽屉。
6. 顶部增加视觉 Hero 区。
7. 增加概览卡片。
8. 增加引导区。
9. 增加加载态、空状态、错误态。
10. 增加局部 canvas、图标、图片、视觉纹理。
11. 优化按钮位置和操作路径。
12. 优化移动端触控体验。

必须保留：

1. 原有页面对应的功能模块。
2. 原有接口调用。
3. 原有字段映射。
4. 原有路由跳转。
5. 原有状态管理。
6. 原有权限逻辑。
7. 原有真实数据来源。

可以改变“页面怎么组织”，不能改变“功能是否存在、数据是否真实、业务是否可用”。

三、添加作物页

目标：
优化为移动端友好的分组表单，让农户填写作物信息时轻松、清晰、有引导感。

允许结构重构：

* 普通表单可以改成分组卡片表单。
* 提交按钮可以改成底部固定操作栏。
* 字段可以按“基础信息 / 种植信息 / 补充信息”重新组织。
* 可以加入轻量农业图标或顶部说明区。

重点优化：

1. 顶部说明区。
2. 基础信息分组。
3. 种植信息分组。
4. 补充信息分组。
5. 输入框、选择框、文本域样式。
6. 必填提示。
7. 错误提示。
8. 面积单位展示。
9. 日期选择。
10. 底部固定提交按钮。
11. safe-area-inset-bottom 适配。
12. 提交 loading / disabled 状态。
13. 成功 / 失败反馈。
14. 键盘弹出时不遮挡输入。

要求：

* 使用 FormField、AppCard、SectionHeader、AppButton。
* 不要像后台表单。
* 不要破坏原有提交逻辑。
* 不要新增无法提交的假字段。需要新增前端辅助字段时，必须说明映射方式。

四、AI 智能问诊页

目标：
优化为可信的农技问诊工具，而不是泛 AI 聊天机器人。

允许结构重构：

* 可以从聊天页或普通输入页重构为“问诊工具流”。
* 可以按照“描述症状 → 上传图片 → AI 分析 → 建议结果”组织。
* 可以使用底部操作栏、结果卡片、分析中状态、风险提示卡片。
* 可以用 canvas 做柔和分析反馈，但不能 AI 霓虹风。

重点优化：

1. 顶部问诊介绍区。
2. 症状输入区。
3. 作物名称 / 症状描述 / 发生时间 / 图片上传等已有字段。
4. 图片上传预览。
5. 上传成功 / 失败 / 删除反馈。
6. 轻量流程提示。
7. AI 分析加载态。
8. 结果展示区。
9. 风险提示。
10. 失败重试。

设计要求：

* 科技感要克制。
* 可以使用农业科技素材或轻量 canvas 分析动画。
* 严禁蓝紫 AI Hero。
* 严禁机器人、芯片、大脑、宇宙科技图。
* 不要做成 ChatGPT 聊天页面。
* 后端只返回一段文本时，不要伪造结构化诊断；可以用漂亮的结果卡片承载。
* 必须保留自然的辅助提示，例如“AI 建议仅供初步参考，实际处理请结合当地农技指导和作物情况。”

五、行情页

目标：
优化为轻量专业的农业数据看板，不是后台价格列表，也不是金融 App。

允许结构重构：

* 表格或普通列表可以改成行情看板。
* 可以增加顶部概览区。
* 可以增加搜索筛选。
* 可以将行情项改为价格卡片。
* 可以加入趋势图、sparkline 或 canvas 微型图表。
* 没有真实图表数据时不能硬编假图。

重点优化：

1. 顶部行情概览。
2. 搜索框。
3. 品类 / 地区 / 涨跌 / 时间等筛选。
4. 行情卡片。
5. 当前价格。
6. 单位。
7. 涨跌状态。
8. 更新时间。
9. 数据来源，如果有。
10. 趋势区域。
11. 空状态。
12. 加载态。
13. 错误重试。

要求：

* 图表必须基于真实数据。
* 没有趋势数据时显示 EmptyState。
* 上涨、下跌、持平使用 StatusChip。
* 不要红绿乱用。
* 不要像金融交易软件那样紧张复杂。

六、销路匹配页

目标：
优化为现代农业供需匹配页面，体现“智能匹配”的价值。

允许结构重构：

* 普通商户列表可以改成供需匹配页。
* 可以增加顶部匹配说明区。
* 可以增加筛选 chip 或底部抽屉。
* 可以把商户项改成推荐卡片。
* 可以加入地图感线条、供需连接视觉、匹配理由。
* 可以用 canvas 做匹配度图或地区连接图。

重点优化：

1. 顶部说明区。
2. 作物、地区、价格、供应量 / 需求量筛选。
3. 一键匹配按钮。
4. 商户 / 收购方卡片。
5. 推荐状态。
6. 需求品类。
7. 收购量。
8. 参考价格。
9. 地区 / 距离。
10. 认证状态或活跃状态，如果有字段。
11. 感兴趣按钮。
12. 联系商户按钮。
13. 查看详情入口。
14. 无结果引导。

重要：

* 后端没有真实匹配度字段时，不要硬编码“92%”这类假数据。
* 可以使用“推荐”“较匹配”“同地区”“品类匹配”等基于已有字段的轻量表达。
* 计算匹配度时，必须基于已有数据并说明计算方式。
* 不要做成电商促销页。

七、感兴趣商户页

目标：
优化为用户的“意向商户 / 收藏商户”管理页。

允许结构重构：

* 普通列表可以改成收藏商户管理页。
* 可以增加顶部统计区。
* 可以增加空状态跳转引导。
* 卡片风格延续销路匹配页。

重点优化：

1. 顶部展示已关注数量。
2. 商户卡片延续销路匹配页风格。
3. 取消感兴趣前二次确认。
4. 空状态引导去销路匹配。
5. 商户信息层级清楚。
6. 操作反馈明确。

要求：

* 不要做成普通后台列表。
* 不要隐藏关键商户信息。
* 不要伪造联系方式或商户数据。

八、营销助手页

目标：
突出“一键生成农产品推广文案”的亮点。页面可以更活泼、更有冲击力，但不能变成廉价促销页，也不能变成 AI SaaS 模板。

允许结构重构：

* 普通表单 + 文本结果可以改成营销工作台。
* 可以设计顶部 Hero 区。
* 可以设计产品信息输入区。
* 可以设计文案风格 chip。
* 可以设计生成中动画。
* 可以设计结果卡片。
* 可以使用农业销售、丰收、农产品市场相关高质量素材。
* 可以使用 canvas 做轻量丰收装饰或生成中动效。

重点优化：

1. 顶部 Hero 区。
2. 产品名称。
3. 产地。
4. 卖点。
5. 目标客户。
6. 使用场景。
7. 文案风格选择。
8. 生成按钮。
9. 生成中状态。
10. 结果卡片。
11. 复制。
12. 重新生成。
13. 保存，如果已有接口支持。
14. 空状态引导。
15. 失败重试。

视觉方向：

* 可以加入农产品、丰收、市场、销售相关高质量公开素材。
* 可以使用少量丰收感、果实感、商品标签、麦穗、市场视觉元素。
* 色彩要克制，不能大面积橙黄红。
* 不要做成促销广告页。
* 不要做成通用 AI 聊天页面。
* 接口只返回一段文案时，用美观结果卡片承载，不要硬编码示例结果当真实结果。

九、通知中心

目标：
优化为清晰、有层级的移动端消息中心。

允许结构重构：

* 普通消息列表可以改成分类消息中心。
* 可以增加顶部分类 tabs。
* 可以增加未读状态和消息类型标签。
* 可以增加空状态引导。

重点优化：

1. 顶部标题区。
2. 分类切换。
3. 全部 / 未读。
4. 系统通知、问诊结果、行情提醒、商户意向等分类，如果后端有 type 字段。
5. 通知卡片。
6. 标题。
7. 摘要。
8. 时间。
9. 类型标签。
10. 未读小圆点。
11. 标记已读，如果已有接口。
12. 全部已读，如果已有接口。
13. 空状态。

要求：

* 没有 type 字段时不要硬做分类。
* 没有接口时不要做假按钮。
* 未读状态要明显但不要刺眼。
* 不要只有“暂无数据”。

十、登录 / 注册页

目标：
优化首屏质感，让用户第一次打开就感到产品专业、美观、可信。

允许结构重构：

* 普通登录表单可以改成欢迎页 + 品牌视觉区 + 登录卡片。
* 可以使用高质量农业场景背景图。
* 可以使用品牌感图标和自然主题视觉。
* H5 端可以居中呈现，App 端以移动端体验优先。

重点优化：

1. 品牌区。
2. 产品名“云上农管家”。
3. 副标题，例如“种植管理、农技问诊、行情销售，一站式智慧农服助手”。
4. 背景图或轻量视觉背景。
5. 登录卡片。
6. 输入框。
7. 登录按钮。
8. 注册入口。
9. 错误提示。
10. loading 状态。
11. 键盘适配。
12. H5 居中和宽度适配。

要求：

* 可以使用高质量农业场景背景图，但必须许可清晰、本地化、压缩并记录。
* 不要使用蓝紫 AI 登录页。
* 不要使用机器人、芯片、抽象科技背景。
* 不要使用随便找来的 logo。
* 没有正式 logo 时，可以使用统一风格开源图标搭配品牌文字。

十一、运营轻后台 H5 页面

目标：
优化为干净、专业、适合比赛展示的轻后台。它不需要强行做成 App 风格，但要和品牌统一。

允许结构重构：

* 后台页面布局可以优化。
* 表格可以在窄屏下转卡片。
* 筛选区可以折叠。
* 数据概览可以卡片化。
* 弹窗、按钮、状态标签可以统一设计。
* 但不能删除管理能力，也不能破坏权限逻辑。

重点优化：

1. 宽屏 H5 布局。
2. 顶部 / 侧边导航。
3. 数据卡片。
4. 表格。
5. 筛选区。
6. 操作按钮。
7. 弹窗。
8. 审核、删除、发布等确认。
9. 窄屏 H5 适配。
10. 表格溢出处理。
11. 可转为卡片列表的移动端展示。

必须保留：

* 管理员权限判断。
* 接口路径。
* 原有功能模块。
* 用户管理。
* 商户审核。
* 行情管理。
* 通知管理。
* 审计日志等逻辑。

禁止：

* 把后台做成花哨 App 首页。
* 重写权限逻辑。
* 删除现有管理能力。
* 为了美观隐藏关键字段。

十二、语音助手入口

如果项目已有语音助手，请优化为不遮挡主流程的辅助入口。

允许结构重构：

* 悬浮入口可以重新设计。
* 展开方式可以改为底部抽屉或半屏面板。
* 可以优化识别状态、推荐动作和确认流程。
* 可以让语音识别结果更好地预填表单。

要求：

1. 默认状态是轻量悬浮按钮。
2. 位置不能遮挡底部导航、表单提交按钮、卡片操作按钮。
3. 适配安全区。
4. 展开时优先使用底部抽屉或半屏面板。
5. 面板包含识别状态、识别文本、推荐动作、确认 / 取消。
6. 用户说“添加橘子 3 亩”时，尽量跳转添加作物页并预填已有字段。
7. 信息不完整时，先填已有字段，再提示补充，不要完全不跳转。
8. 视觉继承设计系统。
9. 严禁 AI 霓虹风和强发光动效。

十三、素材与 canvas 统一要求

新增素材：

1. 不生成图片。
2. 使用许可清晰公开资源。
3. 下载到本地 static / assets。
4. 压缩图片。
5. 更新 ASSET_CREDITS.md。
6. 风格统一。
7. 不热链。
8. 不用版权不明素材。
9. 不用其他 App 截图。
10. 不用带明显品牌识别的素材。

使用 canvas：

1. 只能局部增强。
2. 不能替代表单、按钮、列表和文字。
3. 必须封装组件。
4. 必须有降级方案。
5. 必须控制性能。
6. 页面离开时停止动画。
7. 不遮挡交互。
8. App 和 H5 都要验证。
9. 严禁 canvas 做 AI 霓虹、赛博网格、芯片电路、机器人等偏题效果。

十四、全局一致性收尾

请检查并统一以下内容：

1. 色彩是否全部继承第 0 轮 tokens。
2. 是否还有大量硬编码 hex。
3. 是否出现蓝紫霓虹、荧光绿、强发光等禁用效果。
4. 卡片圆角是否统一。
5. 阴影是否统一。
6. 按钮高度是否统一。
7. 表单样式是否统一。
8. 状态标签是否统一使用 StatusChip。
9. 空状态是否统一。
10. 加载状态是否统一。
11. 错误提示是否统一。
12. 底部导航是否遮挡内容。
13. 语音助手是否遮挡按钮。
14. 图片是否变形。
15. 是否有横向滚动。
16. H5 后台表格是否溢出。
17. 是否有无用 console.log。
18. 是否误改接口、路由、数据字段。
19. 是否新增版权不清晰素材。
20. 是否有过大图片文件。

十五、最终功能验证

请完整验证以下流程：

1. 登录。
2. 注册。
3. 进入我的地。
4. 作物列表加载。
5. 添加作物。
6. 编辑作物。
7. 删除作物。
8. AI 问诊提交。
9. AI 问诊结果展示。
10. 行情数据加载。
11. 行情筛选 / 搜索。
12. 销路匹配加载。
13. 感兴趣商户操作。
14. 营销文案生成。
15. 文案复制。
16. 通知查看。
17. 标记已读，如果有。
18. 管理后台访问。
19. 管理后台各模块切换。
20. App 端底部导航切换。
21. H5 端刷新和路由跳转。
22. canvas 正常显示与降级，如果使用。

十六、本轮禁止事项

1. 禁止重做第 0 轮视觉方向。
2. 禁止推翻第 1 轮样板页风格。
3. 禁止新增版权不明素材。
4. 禁止热链图片。
5. 禁止伪造业务数据。
6. 禁止破坏接口。
7. 禁止删除已有功能。
8. 禁止引入大型 UI 框架。
9. 禁止把后台做成花哨移动 App。
10. 禁止为了美观隐藏关键业务信息。
11. 禁止把营销助手做成通用 AI 聊天页面。
12. 禁止把 AI 问诊做成蓝紫 AI 工具模板。
13. 禁止 canvas 炫技、偏题、影响性能。
14. 禁止不同页面素材风格乱跳。

十七、最终交付

完成后请输出：

1. 本轮修改文件清单。
2. 全部页面结构重构说明。
3. 全部页面视觉亮点总结。
4. 新增 / 修改组件说明。
5. 色彩体系继承检查结果。
6. 是否仍存在硬编码颜色。
7. 新增素材及 license 说明。
8. ASSET_CREDITS.md 更新说明。
9. canvas 使用说明，如有。
10. 是否修改业务逻辑，如有必须详细说明。
11. App 端适配检查结果。
12. H5 端适配检查结果。
13. 核心功能验证清单及结果。
14. 剩余风险。
15. 后续可优化建议。

最终目标：
让“云上农管家”形成统一完整的移动端产品体验。它应该看起来像一个自然、可信、专业、让人眼前一亮、适合比赛展示的智慧农业 App，而不是 AI 生成模板、后台系统、土味农业站、廉价促销页或偏题科技产品。可以大胆重构页面结构、强化视觉表现力，但必须保留原有功能模块、真实业务数据和前后端联调能力。
